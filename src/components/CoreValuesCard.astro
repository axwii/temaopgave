---
const { title, icon } = Astro.props;
---

<li>
  <img src={icon} alt="" />
  <h4>{title}</h4>
  <p>
    <slot />
  </p>
  <p>Read More →</p>
</li>

<style>
  li {
    background-color: #fff;
    border-radius: 20px;
    padding: 1rem;
    align-self: start;
    min-width: 250px;
    /* background: var(--bg-card); */
  }

  img {
    width: 50px;
    height: 50px;
  }

  p:last-of-type {
    font-weight: 600;
    /* color: var(--text-card); */
  } 

</style>
